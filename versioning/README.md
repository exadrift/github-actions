# versioning
versioning can be used to ensure the following set of criteria
- a version file is present
- a change log is present
- the change log head entry matches the current version
- the current version has not already been tagged in the repository

for usage, see the [action.yaml](https://github.com/exadrift/github-actions-agent/blob/main/versioning/action.yaml)
